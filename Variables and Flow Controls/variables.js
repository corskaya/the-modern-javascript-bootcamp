// Lesson

// 1) You can't define a variable more than once
let petName = 'Hal'
console.log(petName)
petName = 'Spot'
console.log(petName)

// 2) First character of variable name, can only begin with a letter, underscore or dollar sign

// 3) Variables names cannot be reserved keywords (e.g. let)
